Business modules.

Each subfolder owns one business capability. Modules should contain handlers,
DTOs, service logic, repository interfaces, PostgreSQL implementations, models,
and tests where applicable.

